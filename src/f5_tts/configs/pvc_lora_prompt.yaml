exp_name: F5TTS_v1_Base
dataset_path: /abs/path/to/prepared_speaker_dataset
speaker_id: speaker_001
base_ckpt: ""
use_ema: true

learning_rate: 1.0e-5
batch_size_per_gpu: 3200
batch_size_type: frame
max_samples: 64
grad_accumulation_steps: 1
max_grad_norm: 1.0
epochs: 100
num_warmup_updates: 20000
save_per_updates: 5000
keep_last_n_checkpoints: 2
last_per_updates: 1000
num_workers: 16
seed: 666
logger: none
checkpoint_dir: ""

lora_rank: 16
lora_alpha: 16
lora_dropout: 0.05
prompt_drop_path: 0.3

output_root: lora
